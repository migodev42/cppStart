#include <iostream>
#include "compute.h"

int main() {
    using std::cin;
    using std::cout;
    using std::endl;

    int rs = myCompute(1, 2);
    cout << "myCompute rs:" << rs << endl;
    return 0;
}
