#include <vector>
#include <string>

using std::vector;
using std::string;

class Solution {
public:
    void backtrack(int n, vector<vector<string>>& rs) {
        // if () {
            // rs.push_back();
        // }
        for(auto i=0; i<n; ++i){
            
        }
    }
    vector<vector<string>> solveNQueens(int n) {
        vector<vector<string>> rs;
        backtrack(n, rs);
        return rs;
    }
};